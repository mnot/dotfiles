
umask 002

export PATH="/usr/local/bin:/usr/local/share/npm/bin:/usr/local/Cellar/ruby/2.6.0/bin/:$PATH:$HOME/Applications:/Developer/usr/bin:/opt/*/bin"
export MANPATH="/usr/local/man:$MANPATH:/opt/*/man"
export SPATH="/sbin:/usr/sbin:/usr/local/sbin:/opt/*/sbin"

# export PYTHONPATH="$PYTHONPATH:/Library/Python/2.7/site-packages"
export GEM_HOME="$(brew --prefix)"
export JAVA_HOME="$(/usr/libexec/java_home)"
export NODE_PATH=/usr/local/lib/node_modules
export saxpath='/usr/local/Cellar/saxon/9.9.0.1/libexec/saxon9he.jar'

if [ -f "${HOME}/.gpg-agent-info" ]; then
   . "${HOME}/.gpg-agent-info"
  export GPG_AGENT_INFO
  export SSH_AUTH_SOCK
  export SSH_AGENT_PID
fi

export CLICOLOR=yes

alias l="ls -Fkloh"
alias la="ls -AFkloh"
alias dnsflush="dscacheutil -flushcache"
alias pstree="ps -axwwco user,pid,ppid,pgid,command | /usr/local/bin/pstree -f - $@" 
alias ldd="otool -L"
alias plop-view="python -m plop.viewer --datadir=."

